enum DaysOfWeek {
  MON, TUE, WED, THU, FRI, SAT, SUN
}
let monday : DaysOfWeek = DaysOfWeek.MON;

// More control over what the compiler
enum DaysOfWeek2 {
  MON = 1, TUE = 2, WED, THU, FRI, SAT, SUN
}
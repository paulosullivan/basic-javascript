"use strict";

x = 10; // This will cause an error because x is not declared

function myFunction() {
    y = 20; // This will also cause an error because y is not declared
}

// You can also enable this feature for a single function by placing "use strict" as the first line of the function.